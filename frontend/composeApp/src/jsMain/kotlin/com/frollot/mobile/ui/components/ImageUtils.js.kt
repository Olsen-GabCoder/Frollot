package com.frollot.mobile.ui.components

import androidx.compose.ui.graphics.ImageBitmap

/**
 * Implémentation JavaScript/Web de la conversion ByteArray -> ImageBitmap.
 * 
 * Note: Cette implémentation est un stub pour permettre la compilation.
 * L'API ImageBitmap de Compose n'est pas directement supportée sur JS.
 */
actual fun ByteArray.toImageBitmap(): ImageBitmap? {
    // TODO: Implémenter avec l'API Canvas du navigateur si nécessaire
    println("⚠️ toImageBitmap non implémenté sur Web")
    return null
}

